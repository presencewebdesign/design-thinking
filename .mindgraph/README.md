# MindGraph

Git-backed AI memory for this repository.

- `config.json` — committed repository defaults
- `local.json` — ignored per-machine sharing override
- `nodes/` — active node records and committed tombstones
- `graph/edges/` — active edge records and committed tombstones
- `memory.db` — ignored local cache
